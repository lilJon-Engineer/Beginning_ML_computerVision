# Take an input vector from number_import.py and compute the dot product with the weight vector. 
# Pass this weight sum 'a' for unit 'i' through a sigmoid squashing function to produce the state of unit i. 
# Squashing funtion is a scaled odd hyperbolic tangent function.
# f(a) = A tanh(Sa) where A = 1.7159 and S = 2/3. (Find out why)
# 

# Cost function C = 1/P sum_p